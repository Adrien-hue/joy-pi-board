# Validation and release strategy

Status: **S00, S01 AND S02 COMPLETE; delivered software subsets PASS locally and in CI, full A/B/C product acceptance NOT RUN**. [S00 evidence](s00-evidence.md) records the local results and verified run 35527458889 for `b6e139c3378cd77fdcb4c5edc610977c08bbf72a`. S01 in-memory contract subsets are verified locally and in hosted run 35637699325 attempt 1 at `ccd1245a64fc372faa4ebc85eb000f78a01bd396`; [S01 evidence](s01-evidence.md) preserves local input provenance separately from CI. S02 is [complete with local and verified hosted CI evidence](sprint-02.md). Actual S02 test subsets are traced in [S02 evidence](s02-evidence.md); full product gates remain incomplete. [S03](sprint-03.md) is implemented and locally verified in the software/automated-engine subsets recorded below; required branded/minimum/mobile/manual evidence and new hosted CI keep closure pending. S04 physical tests remain NOT RUN. Normative for release evidence and requirement traceability (JPB-024). Product budgets are established in [the specification](specification-v0.1.0.md); durations/sampling/statistics remain **Proposed P-06**. Historical shell-only sizes remain separate from the preliminary S03 dashboard measurements. Historical documentary checks are in [documentation review](documentation-review.md).

Numeric expectations are adopted through [ADR-001](adr-001-exact-json-integers.md), not an open library proposal. S00 [shared fixture](../testdata/README.md) byte checks are not B-03 end-to-end validation. Future tests must cover original number tokens, uint64 bounds, nulls and schema-driven float/BigInt conversion across Health → Board → browser → display. S01 approves unknown-member tolerance under global structural checks, independently of numeric exactness.

## Evidence and release rules

Each run records test ID, PASS/FAIL/NOT RUN, UTC date, operator/CI URL, Board source commit, exact binary SHA-256, package SHA-256 if applicable, toolchain/lockfile identities, OS/kernel/architecture, configuration, Health commit/version, client/load settings, raw evidence and any exception. Record compiler flags and embedded asset manifest/checksums in build metadata. A mock is identified as a mock. Hardware model/OS are operator evidence, not fabricated Health API fields.

Release requires **Class A PASS, Class B PASS and Class C PASS**, with every required check executed and passing. A waiver or unexecuted required check does not count as PASS. Relevant P-xx choices must be ratified or replaced explicitly before their dependent gates can be accepted. An unexecuted physical check remains NOT RUN, not inferred PASS from a mock, developer workstation, cross-build or Health's own hardware release status.

Build the candidate once after source checks; identify it with SHA-256. Run binary integration and physical acceptance on those exact bytes. For architecture-specific source/race checks, attach results to the same source/toolchain inputs, then attach A-04, B binary runs and C runs to the ARM64 candidate checksum. Stage the same binary in the Debian package and prove the extracted/installed binary hash matches. Promote/copy the accepted artifacts without rebuilding. A new binary/package or embedded asset change invalidates affected acceptance and requires a new candidate identity; a same-version rebuild is not automatically accepted. Packaging/install scripts and package hash have their own S04 checks.

## Class A — build and correctness

These IDs define product gates. S00 commands now execute the foundation subsets traced below; later functional extensions and full product acceptance remain planned. A passing subset does not mark the whole product gate PASS.

| ID | Planned check and measurable PASS condition | First responsible sprint |
|---|---|---|
| A-01 | Go formatting has no diff; test/vet all first-party packages (`./cmd/... ./internal/... ./web` in S00, excluding Go files inside npm dependencies); native race tests succeed with no race; module verification/tidy leaves tracked files unchanged. No Health internal imports or metric collection. S00 subset results are in the evidence record. | S00, expanded S01/S02 |
| A-02 | Clean locked frontend install; type-check, lint, meaningful unit tests and production Vite build exit zero with no lockfile drift. Parser/types/formatting tests added by S01/S03. | S00 |
| A-03 | Clean native build and `GOOS=linux GOARCH=arm64` cross-build succeed from pinned tools in documented order; approved flags and module metadata recorded; inspect architecture and runtime dynamic dependencies. | S00 |
| A-04 | Exact candidate serves index and every initial embedded asset from an empty working directory without source/dist/Node; each MIME/byte checksum is correct; production CSP works; both compressed resource budgets pass. | S00 smoke, final S04 |
| A-05 | Configuration precedence, invalid URL/listener/port, help/version, signal shutdown and readiness without Health behave as documented; no startup provider dependency; no unsupported runtime tools required. | S02 |
| A-06 | Debian install/start/upgrade/remove review and sandbox checks; dedicated non-root service, valid unit, no Health startup dependency; installed binary checksum equals candidate; metadata/checksums complete. | S04 |

Race tests may run on a suitable Linux amd64 runner with its required toolchain; a successful ARM64 cross-build is not a race-detector run. Native/cross compilation and runtime dependency checks are separate gates. Review commands/build recipes for hidden local assets and unpinned downloads. Development/build Internet access is allowed by scope; runtime Internet dependency is not.

## Class B — contracts, integration and resilience

Use a Board-owned controllable fake Health HTTP server and an injected elapsed/wall clock. Do not sleep for 30 real seconds in boundary tests. Control transport reads, delayed headers, partial bodies, clock advancement and cancellation with barriers/channels. Use real sockets where HTTP parser/cancellation behavior matters. All must assert response headers, absence of leaked details and unchanged last success after failure where applicable.

| ID | Planned cases and PASS oracle | First responsible sprint |
|---|---|---|
| B-01 | Complete and partial valid schema 1.0; complete→partial atomically replaces values including nulls; zero/false preserved; exact hostname/time; no invented fields. Partial remains Health success and Board 200 available/current. | S01 validation; S02 envelope |
| B-02 | Each issue code; exact path order/associations; null-leaf load/memory/root groups; whole network null; interface-local state/RX/TX nulls; unaffected interfaces preserved; all four firmware null flags/same code+message, temperature independent; true flags with empty issues; valid empty interface array with other useful data; reject all-unavailable/no useful data. | S01 |
| B-03 | Round-trip exact `9007199254740993` and `18446744073709551615` through Health bytes → Board object bytes → browser bigint → exact display. Check 0, safe-integer boundary, uint64 max+1, negative, fractional, exponent and quoted integers; invalid cases follow ratified token policy. | S01, browser display S03 |
| B-04 | Unknown schema string; missing/non-string version; invalid/trailing JSON; wrong types/ranges; duplicate decoded members; unknown members accepted without semantics; malformed UTF-8; every required key omitted in turn versus permitted explicit null; forbidden object nulls; `issues: null`; internal_error issue code; missing/duplicate/reordered issues; unsorted/duplicate/oversized interfaces and mismatched firmware issues; invalid cases rejected with exact classification. Unknown members alone are accepted; their contents still obey global limits and Unicode/duplicate rules. | S01 |
| B-05 | Health 65,536-byte/depth-32 valid boundaries, limit+1 rejected, oversized Content-Length and chunked body bounded; separate approved overview 81,920-byte/depth-34 transport limits (S02 harness, production browser consumer S03); HTML-escaping boundary regression. HTTP 500/503/204/redirect → upstream_error; malformed 200 → invalid_response; wrong media/encoding, truncated 200 and unparseable status cover mapping. No raw errors/body text leak. | S02 |
| B-06 | Refused connection versus deadline expiry; DNS/transport failure; delayed headers and stalled/slow-stream body reach same 1 s global timeout; clock/read cancellation bounded. Assert exactly one outbound attempt, including reused-connection failure, no redirect follow/retry and no call before a browser demand. | S02 |
| B-07 | Never-success, success, failure at 29.999 s / 30 s / 30.001 s, expiration during a request, repeated failure, recovery and process restart. Correct current/stale/none, unchanged success time on error, reset on partial success, snapshot null after expiry. Advance wall clock both directions while elapsed age controls expiry. | S02 |
| B-08 | Simultaneous callers and data races; ratified P-04 sharing only active flight; cancellation of one/all waiters; orphan ends by deadline; no completed-flight reuse; late completion ignored; deterministic sequence/commit ordering and immutable cache copies. Next nonoverlapping demand starts new attempt. | S02 |
| B-09 | Board routing/method/query/body/error precedence, HEAD wire behavior, no SPA fallback for API errors, no-store, MIME/security headers and cache separation; Health failure produces Board 200; Board's own defect produces Board HTTP error. No idle autonomous upstream traffic; logs contain transitions, no snapshots/poll-success spam. | S02, security/assets S04 |
| B-10 | Frontend initial/nominal/partial/stale/none/recovered/Board-unreachable states; immediate load, 5 s cadence, no overlapping requests/retry; abort and late-response discard. Advance clock beyond 30 s with Board down and assert values disappear without any server response. Test header age/RTT, first-load stale, missing age, browser/Pi clock skew, local wall jumps, visibility/freeze/resume and delayed callbacks. | S03 |
| B-11 | CPU 0.5037 displays ~0.5%; bigint counters remain exact and labeled cumulative; null/zero-total percentages; false/null distinction; active/since-boot labels; issue rendering as text (including markup-like strings). 360 px no horizontal overflow, long fields, keyboard/focus, zoom, non-color state cues, no arbitrary score/threshold. | S03 |

Reference upstream tests (read as evidence, not run here): `TestCoordinatorMapsExpectedReasonsInFrozenOrder`, `TestCoordinatorMapsInterfaceLocalFailure`, `TestEncodePartialSnapshotPreservesIssueOrderAndNulls`, `TestEncodeRejectsInvalidSnapshots`, `TestProviderOutcomeMapping`, plus exact-integer tests. Their pinned files and verified facts are in [Health evidence](health-baseline.md). Create Board-owned fixture expectations; do not import upstream test helpers/internal packages.

## Proposed reproducible performance protocol

Use an actual **Raspberry Pi 3B+**, Raspberry Pi OS 64-bit / Trixie, Linux ARM64 for Class C, real Health and the exact candidate binary. Record Pi cooling/power/storage, governor, ambient conditions, other services, OS/kernel, Health identity/configuration and LAN connection. Keep the environment consistent; do not remove failing samples because the machine was busy. If external interference invalidates a run, preserve it and rerun with the reason recorded. Health's hardware acceptance is a separate project obligation.

The operator's measurement tools may read process/kernel counters; that is an external validation harness, not Board system-observation functionality. Board itself does not gain `/proc` collection. Clients/load generators run on another trusted LAN machine so their CPU/RSS do not contaminate Board's process figures.

### Process scope and phases

Collect Board **process** RSS (not virtual size, not combined with Health/browser/journald) every 1 s; use a process identity/start-time check to detect restarts. Stable RSS PASS: maximum observed sample after warmup ≤50 MiB, record median/p95/max and start/end trend. This operationalizes “stable” conservatively; do not hide a growing process behind a mean. Count all Board threads' user+system CPU seconds; mean percent = `100 * CPU_seconds_delta / elapsed_seconds`, one-core denominator. Record 1 s samples and full-phase mean. Exclude only the stated warmup, never failure-recovery sections from their own results.

| Phase | Warmup / measured interval | Gate |
|---|---|---|
| No clients, Health running | 2 min / 10 min | CPU mean ≤1%, RSS ≤50 MiB; upstream request count remains zero |
| One client, exactly one overview start every 5 s | 2 min / 10 min (120 scheduled starts) | CPU mean ≤2%, RSS ≤50 MiB, nominal p95 ≤500 ms; no overlapping client requests |
| Ten clients, staggered by 0.5 s at 5 s each | 2 min / 10 min (1,200 scheduled starts) | Stable process, no Board errors/skips under nominal service, valid overviews, RSS ≤50 MiB, proposed nominal latency gate per client and aggregate |
| Ten clients, synchronized 5 s starts | 2 min / 10 min (1,200 starts) | Same capacity checks, sharing/order behavior if P-04 adopted; no unbounded queues |

For ten clients, record CPU mean/p95 and report it without inventing a new approved CPU limit. Applying the established nominal latency gate in both load phases is a proposed acceptance interpretation. Any skipped scheduled start under nominal conditions fails the capacity run; record failure/timeout counts, not just successful latency. Fake-server load tests supplement, never replace, real-Health Class C phases.

### Latency and lifecycle

Use a monotonic client timer from request dispatch to complete response body receipt/validation; record every sample, bytes/status and state. Sort N samples; p95 is the nearest-rank value at index `ceil(0.95*N)` (1-based), with no interpolation. Report N, min/median/p95/max and failure count. Do not exclude errors/timeouts from reports to improve percentiles. Transport readiness probes and performance timers are external harness traffic explicitly recorded as such.

- Nominal overview: use the 120 one-client samples and each ten-client run; real Health must return valid nominal responses. If Health is failing, report that scenario separately and rerun nominal measurement with the reason recorded.
- Board overhead: collect ≥200 requests at 5 s cadence after 2 min warmup. Proposed timing seam in the **same candidate binary**, opt-in diagnostics only, records handler start/end and upstream network start/complete-body-read (validation excluded from the upstream interval). Calculate handler duration minus upstream network wait; include decode/validation/serialization in Board overhead. Use a single client without shared flight for unambiguous attribution. p95 ≤50 ms. Measure real Health and a controlled responder; preserve both labels. Diagnostic overhead is included, so the estimate is conservative. Review this timing seam in S02; do not rebuild an “instrumented equivalent” after candidate acceptance or enable per-poll logs by default.
- Immediately detectable refusal: stop Health/listener, verify loopback connection refusal rather than packet drop; ≥200 requests at 5 s after 20 excluded warmup attempts, p95 ≤250 ms for complete Board response, HTTP 200 with `connection_failed`. Keep exact candidate unchanged.
- True timeout: controlled server accepts and withholds headers/body (also test unreachable packet-drop path where safe). At least 30 attempts per timeout mode; assert 1 s configured global deadline and cancellation. A proposed integration scheduling tolerance ≤100 ms beyond the deadline is recorded separately; it does not redefine the 1 s timeout or apply the 250 ms refusal gate. Fake clock tests assert the exact deadline without scheduler tolerance.
- Readiness: 20 fresh starts with Health stopped; measure process spawn to successful `/` and required embedded asset response, probing every 20 ms; every trial ≤2 s. Also run with Health present. No overview success is required for readiness.
- Shutdown: 20 SIGTERM trials, including idle and in-flight 1 s Health timeout, from signal to process exit; every trial ≤5 s, no orphan work. Repeat unit/service signal smoke via systemd.

### Resource-size protocol

KiB means 1,024 bytes. Build production/minified assets from locked sources. Proposed compression: gzip level 9, deterministic header (mtime 0/no source filename); report compressor/version. Count each initial URL once on an empty browser cache, no service worker. JS+CSS is the sum of compressed bytes of all eagerly needed JS/CSS, including transitive imports, modulepreload and inline content attributable to these types. Do not game the budget with lazy-loading essential first-page content.

All-initial resources additionally include index HTML, icons/images/fonts if any and the first overview response; use the complete documentation fixture and report its byte size (API may remain uncompressed, then count identity bytes). Include any resource loaded before the initial overview is usable. Exclude HTTP/TLS headers and later periodic overview responses. Record an additional stress size for the maximum accepted snapshot, without asserting all valid snapshots equal the nominal fixture. The inclusion of the first API response is a conservative proposed interpretation of the established 500 KiB budget.

Validate the deployed candidate's actual `Accept-Encoding: gzip` response inventory, not only a directory total. Verify identity fallback and `Vary`; compare served payload checksums with build inventory. JS+CSS ≤250 KiB and all initial resources ≤500 KiB. Record uncompressed sizes too. No external dependency may be excluded simply because it came from a CDN; runtime remote content is forbidden outright.

## Class C — operator acceptance on Pi 3B+

Every row is currently **NOT RUN**. Preserve raw samples/screenshots/log extracts in a candidate-specific future evidence directory; no fabricated result files are created now.

| ID | Future operator procedure / PASS evidence | Sprint |
|---|---|---|
| C-01 | Verify downloaded binary/package checksum and installed binary hash, architecture, Trixie/ARM64, unprivileged systemd identity and embedded assets. Start with real Health stopped: UI/readiness gate passes, overview is unavailable/none with null success; help/version identify candidate. | S04 |
| C-02 | Execute idle, one-client and both ten-client phases with real Health using protocol above; retain CPU/RSS/latency/request-count series and all computed gates. | S04 |
| C-03 | Obtain real data, stop Health, observe Board stays reachable, stale and age then no metrics after >30 s with last success retained; restart Health and observe automatic recovery. Restart Board while Health stopped and confirm lost cache. Stop Board and confirm browser independently ages out data; restart and recover. Partial modes additionally use controlled fault fixtures, labeled synthetic. | S04 |
| C-04 | Run readiness/shutdown/refusal/true-timeout/overhead procedures with identified real or controlled provider per case; meet each budget, no timeout/refusal confusion. | S04 |
| C-05 | Mobile/tablet/desktop UI at documented widths and ≥360 px; long fields/counters, keyboard and non-color cues. Disconnect WAN while retaining LAN and Health: load with cold cache and continue refresh; no remote fetches or fonts. Existing `.local` resolution is tested only if available; access by IP must work. Verify CSP/nosniff/referrer policy. | S04 |
| C-06 | Journal contains startup/shutdown and significant failure/recovery transitions without snapshot/per-poll success logs. Re-check hashes after all runs; package install/upgrade does not replace the accepted binary with different bytes or require live Health. Complete evidence ledger and release review. | S04 |

The operator may need to stop/start test services during **future** acceptance; this document does not execute or authorize deployment in this S03 implementation mission.

## Requirement traceability

Each row maps an established requirement to planned verification and the sprint responsible for delivery. Detailed case matrices above define the oracles. A spanning row does not mark all its delivery sprints complete. The S00 evidence mapping below records the completed foundation scope separately.

| Requirement | Planned verification | Delivery sprint(s) |
|---|---|---|
| JPB-001 | B-09, B-11 route and single-provider review | S02, S03 |
| JPB-002 | B-01, B-11, C-05 metric inventory | S01, S03 |
| JPB-003 | A-01 dependency/source boundary review | S00–S04 |
| JPB-004 | A-01 scope review, B-09/B-11 absent extra actions/routes | S00–S04 |
| JPB-005 | A-01–A-04 stack/build/asset review; S03-T13 frontend regression | S00, regression S03 |
| JPB-006 | A-05, B-09, S03-T01/T10 same-origin consumer; C-01/C-05 | S00 design, S02, S03 consumer |
| JPB-007 | A-05, B-10, C-01/C-04 | S02, S03 |
| JPB-008 | B-06/B-08/B-09, C-02/C-04 | S02 |
| JPB-009 | B-01/B-07/B-08, C-03 | S02 |
| JPB-010 | B-07, C-03 | S02 |
| JPB-011 | A-01 race, B-08 | S02 |
| JPB-012 | B-05/B-09 server; S03-T01/T02/T07 consumer; C-03 | S02, S03 consumer |
| JPB-013 | B-07/B-10, C-03 | S02, S03 |
| JPB-014 | B-01/B-04, pinned evidence review; S03-T02/T03 reuse | S01, regression S03 |
| JPB-015 | B-03/B-11 | S01, S03 |
| JPB-016 | B-02/B-04; S03-T03/T08/T09 issue presentation | S01, S03 consumer/UI |
| JPB-017 | B-10, C-03 | S03 |
| JPB-018 | B-10/B-11, C-05 | S03 |
| JPB-019 | B-03/B-11, C-05 | S03 |
| JPB-020 | A-04, B-06, C-01/C-02/C-04 | S04, seams S02 |
| JPB-021 | A-04, B-09/B-11, C-05 | S02–S04 |
| JPB-022 | B-09, C-06 | S02 |
| JPB-023 | A-03/A-05/A-06, C-01/C-06 | S00, S04 |
| JPB-024 | A-01–A-06, B-01–B-11, C-01–C-06; checksum/release review | S04 |
| JPB-025 | Historic S00/S01 closure evidence preserved; S02 implementation scope/diff review and local evidence; S02-T12 exact-source hosted CI verified (S02-CI-01–S02-CI-06); S03 preparation/scope audit, future S03-T14; no S03/S04 implementation | Documentary foundation, S00, S01, S02, S03 preparation |

Future test implementations should cite these IDs in case names/comments or a manifest so evidence can be audited without guessing. Keep this mapping current if a sprint or proposal changes.

### Completed S00 foundation trace

Evidence IDs refer to [S00 evidence](s00-evidence.md). CI-01–CI-05 belong only to run 35527458889 and its exact tested commit; L-xx identify the separately preserved local runs. This table adds execution status without changing requirement scope.

| Requirements / planned gates | Responsible tasks | Executed evidence / status | Remaining scope |
|---|---|---|---|
| JPB-003/JPB-004; A-01 boundary review | S00-03, S00-09 | Local source review, L-12; no Health imports, collectors or functional scope added. PASS for S00. | Repeat boundary review as S01–S04 introduce code. |
| JPB-005; A-01–A-03, A-04 shell subset | S00-02–S00-06, S00-08–S00-09 | L-01–L-11 and CI-01–CI-05: exact tools, clean builds, source/race tests, embed smoke and preliminary sizes. PASS for foundation. | Final dashboard, interactive/browser/mobile checks and production resource acceptance NOT RUN. |
| JPB-006; configuration design | S00-01, S00-07 | Approved defaults and implemented minimal flags recorded in foundation choices; L-03/L-05 and CI-02/CI-04 cover shell behavior only. DONE for S00. | A-05 full production configuration and B-09 functional API NOT RUN (S02). |
| JPB-023; A-03 standalone ARM64 build subset | S00-05, S00-09 | L-04/L-09/L-10 and CI-03: cross-build/ELF inspection. PASS for build only. | ARM64 hardware execution, A-05/A-06 packaging/lifecycle and C-01/C-06 NOT RUN. |
| JPB-025; sprint evidence and scope | S00-08, S00-10 | Verified foundation/race success, exact commit/image/provenance and documentary exit review. DONE; S00 closed on 2026-09-20. | S01 was ready/not started at S00 closure; later S01 evidence is separate. No release/deployment. |

JPB-024's release gate remains unsatisfied: S00/S01 subsets do not accept full Class A/B or any C-01–C-06 gate. At S00 closure interactive visual verification, complete browser-family validation and mobile visual acceptance were **NOT RUN**. Later S03 engine/image subsets are recorded below; required branded/floor/mobile/manual proof and Raspberry Pi physical acceptance remain incomplete.


### Completed S01 contract trace (local and verified CI evidence)

| Requirement / gate subset | Responsible S01 task | Actual verification | Deferred / NOT RUN |
|---|---|---|---|
| JPB-014; B-01 snapshot validity | S01-01/S01-02/S01-03 | Shared Go and TS corpus: complete/partial, hostname/timestamp, domain and presence checks. Local PASS; S01-CI-02 PASS. | Provider HTTP status and complete→partial cache replacement: S02. |
| JPB-016; B-02 issue semantics | S01-02–S01-04 | All issue families/codes, exact association/order, firmware equality, useful observations and interface-local failure. Local PASS; S01-CI-02 PASS. | Live Health HTTP integration: S02. |
| JPB-015; B-03 exact-number subset | S01-02–S01-04 | uint64/bigint extremes, independent raw copy, Go object serialization actually parsed by TS, exact decimal primitive. Local PASS; S01-CI-02 PASS. | Browser-family execution and dashboard display: S03; production overview transport: S02. |
| JPB-014/JPB-016; B-04 consumption policy | S01-01–S01-04 | Shared invalid/valid corpus, all required-field omissions, approved unknown/duplicate/Unicode/size/depth/version rules, seeded differential mutations, bounded Go fuzzing. Local PASS; S01-CI-02/S01-CI-03 PASS. | Actual browser-family execution NOT RUN. |
| JPB-003/JPB-004/JPB-005/JPB-023; A-01–A-04 foundation regression | S01-05 | Locked install, format/types/lint/test/vet/modules, native/cross build, race, embed smoke and separate parser/shell size reports. Local PASS; S01-CI-01–S01-CI-05 PASS. | Final UI budgets, production configuration/package and all physical checks. |
| JPB-025; scope and honest status | S01-05 | Preserved local working-tree fingerprint; verified exact hosted SHA, attempt, job scope/environment and closure. S00 evidence retained separately, no Health modifications or S02–S04 behavior. DONE. | Later S02 implementation has separate authorization and evidence; publication/deployment remain unauthorized. |

Actual test names/commands/counts, original fixture provenance and limitations live in [S01 evidence](s01-evidence.md) and [shared fixtures](../testdata/README.md). S01's test-only envelope is not `/api/v1/overview`; its Node execution is not browser acceptance. At S01 closure, B-05–B-11 and C-01–C-06 were NOT RUN. Later S02 and S03 software subsets are recorded separately below; full B-10/B-11 acceptance remains incomplete and all C gates remain NOT RUN.

## Prepared S02 validation matrix

Authority: [Sprint 02](sprint-02.md), **COMPLETE, WITH LOCAL AND VERIFIED CI EVIDENCE**. This original matrix retains stable test IDs and acceptance assertions; execution status and actual test names are in the S02 trace below and [evidence](s02-evidence.md). The user ratified the prepared S02 P-02/P-04/P-05 recommendations in the implementation instruction. The existing B IDs keep their product meaning; S02-Txx refine subsets, never replace B-10/B-11 or physical acceptance. P-06 measurement protocols remain proposed.

| Planned test | Tasks | Requirement / gate | Deterministic oracle and necessary real-HTTP coverage |
|---|---|---|---|
| S02-T01 — configuration/actions | S02-02, S02-06 | JPB-006/007/023; A-05 | Table-test absent/present-empty env, valid/invalid flags, duplicate/unknown/positional arguments, precedence and informational action conflicts. IP/IPv6 listener incl port 0, occupied port, endpoint host/port/path/scheme/credentials/query/fragment/encoding. Help/version and invalid config bind/contact nothing; diagnostics omit raw values. No Health probe on construction/start; shell remains available without Health. |
| S02-T02 — single-attempt transport | S02-03 | JPB-008/012; B-05/B-06 | Simulated Health for 200/204/3xx/500/503/101, ignored interim 1xx followed by final status, wrong/missing/duplicate MIME, bad charset/content encoding/trailers, reset before/after headers, truncated Content-Length and chunk framing. Count dial calls, accepted sockets and complete GETs separately. Redirect target and proxy trap receive zero calls. Dedicated fresh connections never replay after reset; no second DNS-address dial or cookie/browser header forwarding. |
| S02-T03 — bounds and classification precedence | S02-03/S02-05 | JPB-008/012/014/016; B-05/B-06 | Body 65,535/65,536/65,537 bytes, known oversized length and streamed unknown length; at most limit+1 bytes collected. Header bound and malformed status are transport failures. Complete unknown schema vs unknown+malformed/duplicate/overdeep JSON. First terminal outcome persists; deadline wins at equal cutoff, non-200 already committed does not turn into timeout, 200 malformed-body non-timeout is invalid_response. Lifecycle/caller cancellation never masquerades as a provider reason. |
| S02-T04 — actual deadline/cancellation wiring | S02-03/S02-06 | JPB-008/020; B-06/A-05 | Fake timer proves exact one-second budget starts at flight installation and never resets on headers/body progress. Separately named real-time tests gate a real httptest server before headers, mid-body and on slow progress; confirm request context/socket closes and one attempt only. Loopback refusal is separately classified, not subjected to timeout latency expectations. Record durations; harness watchdog is not a production-budget change. |
| S02-T05 — state and time | S02-04/S02-05 | JPB-009/010/013; B-01 envelope/B-07 | Never-success, complete, partial replacement incl nulls, each repeated error, recovery on next demand, new coordinator/restart empties cache. Fake elapsed ages **29.999 s / 30 s / 30.001 s** select stale/stale/none on failure, preserving success wall time. A failed one-second attempt can cross the freshness limit. Wall jumps both ways have no age effect; elapsed regression fails closed. Advance clock with no requests: no Health activity. |
| S02-T06 — sharing and cancellation | S02-04 | JPB-008/011; B-08 | Barrier-start ten callers: one active flight/one outbound GET; first caller cancellation leaves others live. All leave: orphan finishes or times out at original deadline without follow-up; later caller may join only if still active. After done publication, a caller starts a fresh attempt immediately, even milliseconds later. Fake ten-client 5 s demand waves prove bounded sharing/counts, not hardware throughput. Cancelled-before-admission caller starts nothing. |
| S02-T07 — ordering and immutability | S02-04/S02-05 | JPB-009/011/013; A-01 race/B-07/B-08 | Gate success/failure submission against deadline/shutdown; terminal signal exactly once and validation-completion anchor preserved through publication contention. Late old success cannot replace newer success/partial or alter last_success_at/logs. Pause an old waiter's finalization while a later flight commits: its reason/snapshot/anchor remain one coherent view. Mutation of input/returned slices/projection cannot change cached/response bytes. Block a writer and prove next demand/publication proceeds without I/O under lock. Test worker/request permit cleanup and exhaustion with cancellation-ignoring fakes released in cleanup; race detector on native supported platform. |
| S02-T08 — HTTP envelope and age | S02-05 | JPB-010/012/013; B-01/B-07/B-09 | Assert exact required fields/enums/nulls and object snapshot, HTTP 200 on all provider failures, JSON/no-store and no internal detail. Header absent before first success, canonical integer and ceil rounding, present after expiry, cap/overflow handling. Gate pre-encoding/finalization across 30 s: failed view becomes none; very late successful view becomes safe Board 503, never available/none. generated_at and header/state share one generation cut; no wall-order assumption. No partial 200 on encoding defects. |
| S02-T09 — real Go HTTP → TS exactness | S02-05/S02-07 | JPB-014/015/016; B-01/B-03 HTTP subset/B-05 | Use controlled Health and real composed Board listener; fetch actual overview bytes with a Node test harness using the evaluated lossless library and S01 parseSnapshot. Replay valid common corpus and representative invalid/provider outcomes. Assert exact uint64 zero, 2^53 boundaries and max decimal primitives; floats/nulls/issues unchanged. Include TestRoundtripAtLimitWithMarkup-derived 65,536-byte `<>&` case, Unicode/unknown members and snapshot depth 32 → envelope depth 34. Count wrapper/newline bytes; bound+1/depth+1 envelope harness rejects. Keep Go raw-object assertions with UseNumber; do not use ordinary JSON numbers as oracle. No live Health or real browser execution is implied. |
| S02-T10 — routing, security and capacity | S02-05/S02-06 | JPB-001/006/012/020/021; A-04 subset/B-09 | Exact /, generated asset and overview routes; missing asset/API path, encoded aliases, traversal, trailing slash, absolute-form, method/Allow, HEAD body suppression, OPTIONS, bare/nonempty query, body/Expect/chunked rejection. Bad requests produce zero Health calls. Distinct safe Board 400/404/405/500/503; parser-level rejection may close/non-JSON. 32 request and 64 connection bounds, 33rd/65th overload paths, no unbounded queue; retain actual compiled MIME/CSP/nosniff/referrer and no-store/identity behavior. Header/read/write/idle limits and permits tested separately from Health deadline. |
| S02-T11 — lifecycle and logs | S02-06 | JPB-007/008/022/023; A-05/B-06/B-09 | No Health at startup/idle; readiness without Health. Start process, hold upstream work and issue SIGTERM: admission closes, provider cancels, waiters release and exit stays within 5 s. One absolute shutdown deadline incl forced close; second signal path. Invalid startup and shutdown exits distinguished. Capture transition-only logs: same failure and ordinary success do not repeat, recovery/issue code-path changes do. Malicious body, issue message, URL credential and raw system error never appear. Block sink and exhaust bounded queue; HTTP/shutdown do not wait indefinitely. |
| S02-T12 — regression and evidence | S02-07/S02-08 | JPB-003/004/005/023/024/025; A-01–A-04 subsets and S02 closure | Existing locked install/format/types/lint/tests/vet/modules/fuzz/race, native and Linux ARM64 builds, frontend-before-embed, standalone smoke/no runtime Node and missing-dist checks. Update smoke expectation for actual API without losing shell byte assertions. New hosted run must test exact S02 code and include T01–T11; preserve local vs CI hashes and real vs fake timing. A workflow edit is not a successful run; no Pi, final dashboard or full A/B/C claim. |

Clock/barrier implementation and the limited real-time cases are specified in [S02 test approach](sprint-02.md#test-approach-evidence-and-closure). Use common testdata as input authority; derive cases using exact bytes/integer tokens rather than adding independently edited Go/TS copies. A controlled HTTP Health substitute is explicitly synthetic; live Health and physical resource budgets remain S04 evidence. No passing socket mock can satisfy C-01–C-06.

The S02 closure requirement is satisfied by the preserved local ledger and [verified hosted run](s02-evidence.md#verified-hosted-ci) 35908782107 attempt 1: successful foundation/race jobs, exact SHA, confirmed test/script coverage, observed environments and artifact provenance. No S00/S01 result is relabeled S02. At S02 closure B-10/B-11, target-browser execution and mobile/visual acceptance were NOT RUN; later S03 subsets are recorded below. Debian/systemd and physical C gates remain NOT RUN. P-06's performance definitions must be ratified before the corresponding measurements, not by preparing this matrix.

## S02 local execution trace

This stable section anchor retains the original local trace. Local results refer to the historical dirty-source fingerprint in [S02 evidence](s02-evidence.md); the separate CI column refers only to [run 35908782107 attempt 1](s02-evidence.md#verified-hosted-ci) at `a11f07c85ff4071a13ca3ae9227920e9f1c7dda4`. Coverage was checked against the workflow, scripts, test entry points/skip conditions and job logs, not inferred from the overall green status. Nonverbose Go package results establish suite success without supplying new per-test counts or timings. Planned oracle rows above are specifications; full A/B/C acceptance remains incomplete.

| Matrix / requirements / gate subset | Implemented assertion sources | Local status / remaining scope | Verified S02 CI |
|---|---|---|---|
| S02-T01; JPB-006/007/023; A-05 | config.TestConfiguration; main.TestSafeStartupExitCodes, TestStartupAndShutdownWithHealthAbsent, existing help/invalid-argument tests | PASS. Actual Pi readiness remains C-01/C-04 NOT RUN. | S02-CI-02 / S02-CI-06: PASS |
| S02-T02/T03; JPB-008/012/014/016; B-05/B-06 | health.TestTransportPolicy, TestBodySizeBoundary, TestCountedBodyLimitAndEarlyTransportTimeout, TestWireFailuresAndNoReplay, TestSingleAddressProxyAndRedirect; overview.TestDeadlinePriorityAndImmutableViews; HTTP corpus | PASS: statuses, MIME/encoding/trailers, framing/header/body bounds, exact cutoff and S01 classifications. | S02-CI-02 / S02-CI-03 / S02-CI-06: PASS |
| S02-T04; JPB-008/020; B-06/A-05 | overview.TestRealDeadlineHeadersBodyAndProgress; health.TestCancellationAndRefusal; fake timer tests | PASS. Measured durations identify workstation/network harness; no Pi percentile claim. | S02-CI-02 / S02-CI-06: PASS |
| S02-T05; JPB-009/010/013; B-01 envelope/B-07 | overview.TestStateTimeAndReplacement, TestClockRegressionAndShutdownRace | PASS: all five reasons, never-success/restart, full/partial replacement, recovery, exact boundaries and wall jumps. | S02-CI-02 / S02-CI-06: PASS |
| S02-T06; JPB-008/011; B-08 | TestActiveSharingAndCancellation, TestTenCallersShareOneRealHealthGET, TestOrphanCanBeRejoinedAndStopDoesNotInventReason, TestOrphanTimeoutLateWorkerAndSaturation | PASS: three fake 5 s waves, ten waiters/one real GET per wave, first/all cancellation, orphan deadline/rejoin, no completed reuse. Physical sustained ten-client load remains NOT RUN. | S02-CI-02 / S02-CI-06: PASS |
| S02-T07; JPB-009/011/013; A-01 race/B-07/B-08 | TestValidationAnchorAndLatePublication, TestDeadlinePriorityAndImmutableViews, TestClockRegressionAndShutdownRace, TestRequestCapacityAndBlockedWriters; Linux race | PASS: validation anchor, late rejection, coherent old views, 32 unfinished-worker and 32 request bounds, atomic terminal races and unlocked writes. | S02-CI-02 / S02-CI-06: PASS |
| S02-T08; JPB-010/012/013; B-01/B-07/B-09 | TestResponseGuardsAndAgeArithmetic, TestDelayedFinalization, TestExpirationDuringAssembly, TestMaximumMarkupEnvelope | PASS: canonical ceil/capped header, absent-before-success/present-after-expiry, delayed current Board 503, clock defects 500, bounded in-memory output. Browser transit/suspension belongs to S03; see its separate local/manual evidence below. | S02-CI-02 / S02-CI-06: PASS |
| S02-T09; JPB-014/015/016; B-01/B-03 HTTP/B-05 | TestHTTPTypeScriptInterop + scripts/http-contract.mjs; S01 immutable/raw transport regression | PASS: 266 real HTTP attempts, 78 accepted/188 rejected, exact uint64, markup/Unicode, depth 34 and envelope-limit guard. Node execution is not target-browser execution or dashboard display. | S02-CI-03: PASS (Node, not browser) |
| S02-T10; JPB-001/006/012/020/021; A-04 software/B-09 | TestRoutesErrorsAndHeaders, TestConnectionCapacityAndHTTPParser, TestRequestCapacityAndBlockedWriters, TestServerDeadlineWiring, TestRealHeaderAndWriteTimeouts; retained httpui tests | PASS: routes, HEAD/Allow/query/body/Expect, 33rd request/65th connection, parser/security/identity headers and actual read/write/idle deadline wiring. S04 compression NOT RUN. | S02-CI-02 / S02-CI-05 / S02-CI-06: PASS |
| S02-T11; JPB-007/008/022/023; A-05/B-06/B-09 | main.TestRealSIGTERMProcess, TestBlockedLogShutdownBudgetAndSecondSignal, TestStartupAndShutdownWithHealthAbsent; eventlog.TestBlockedSinkAndSanitizedErrors; overview.TestTransitionLogsAndLateResultDiscard; standalone smoke | PASS: native Linux signal test, one absolute stop budget, immediate second signal, bounded blocked logs/no raw details, transition deduplication and no startup/idle Health calls. Installed systemd/journald acceptance remains S04 NOT RUN. | S02-CI-02 / S02-CI-05 / S02-CI-06: PASS |
| S02-T12; JPB-003/004/005/023/024/025; A-01–A-04 subsets | Locked install, check/contract/fuzz, native and ARM64 build, race, smoke, missing-dist, sizes, documentary audit | Local PASS; exact-commit hosted CI PASS; S02 closure satisfied. No release or physical acceptance. | S02-CI-01–S02-CI-06: PASS; documentary review recorded separately |

## Prepared S03 validation matrix

Status: **S03 implemented; local execution ledger and remaining closure checks below**. [Sprint 03](sprint-03.md) owns dependencies, approved consumer/scheduling realization and required closure evidence. [UX](ux-v0.1.0.md#proposed-s03-page-and-component-contract) owns the approved S03 P-06 presentation/accessibility subset. This extends B-03 display/B-10/B-11 and relevant A regressions, not the approved server contract. Existing S01/S02 PASS evidence remains separate; Node HTTP interop is not a passed browser/UI assertion.

| Test ID / tasks | Requirements / gates | Planned procedure and required oracle | Status |
|---|---|---|---|
| S03-T01 — bounded acquisition; S03-01 | JPB-006/007/012/021; B-09 consumer, B-10 | Controlled stream/Fetch tests: same-origin GET only, no query/body/retry/redirect; 200 vs Board 400/500/503 vs no HTTP response, MIME/charset/encoding/no-store, absent/bad/oversized Content-Length, empty/truncated body, read failure, slow headers/body, 81,919/81,920/81,921 bytes, split multibyte UTF-8/BOM/invalid bytes. Prove collected bytes <=81,920, reader cancellation/release and 2 s deadline through parsing. Outer read-bound acceptance is separate from wrapper/schema validity. Error text never echoes arbitrary bytes. | Local implemented assertions PASS; see dated scope below |
| S03-T02 — envelope/header; S03-01 | JPB-012/013/014/017; B-10, B-01/B-09 consumer | Remove each required field, explicit null at every position, wrong enums/types, all valid state combinations and contradictions. Check real Gregorian dates, year 0000/9999, Board zero vs Health nonzero timestamps, fractions and wall-order inversion; never timestamp-difference expiry. Extra Board members rejected under approved S03-R01, extra Health members accepted under P-03. Duplicate root/health/unknown nested keys including escaped equivalents, Unicode, trailing document, depth 34/35. Header absent/present rules, 0/30000/30001/cap/cap+1, signs/fraction/exponent/leading zeros/comma duplicates; current/stale with >30000 rejected, none with success and <=30000 rejected. Contract failure masks old values immediately. | Local implemented assertions PASS; see dated scope below |
| S03-T03 — exact subtree and corpus; S03-01 | JPB-014/015/016; B-03/B-04 regression | Reuse authoritative S01 corpus/expectations; derive wrappers byte-wise, never numeric parse/stringify. Reorder all wrapper properties and escaped-equivalent known names; snapshot first/middle/last, nested marker strings, braces/quotes, prototype-like keys and untrusted isLosslessNumber/toJSON members. Round-trip extremes through real S02 HTTP. Nested snapshot 65,535/65,536/65,537 bytes and depths 31/32/33, wrapper 1,024/1,025 including newline; enforce both limits. Max markup/Unicode snapshot and 9007199254740993/18446744073709551615 preserve exact original tokens with no HTML-escape expansion; S01 validator is reused. Mutating a returned view must not change future accepted state. | Local implemented assertions PASS; see dated scope below |
| S03-T04 — scheduling/deadline/order; S03-02 | JPB-007/017; B-10 | Inject timers, request promises and paired clock: immediate initial visible attempt, slots 0/5/10 s, one active request, skip occupied/missed slots, no catch-up, no retry on completion/error. Fractional mount/slot times and premature callbacks must launch nothing before due, rearm only one positive timeout, and consume each slot once even with fast responses; late callbacks skip fully missed slots. At 1,999/2,000/2,001 ms verify deadline priority, body and post-parse checks. Cancelled generation, delayed timeout callback and late old resolution cannot publish. Hold cancellation cleanup and verify no second reader; release resources in test cleanup. StrictMode setup/cleanup/setup has one surviving initial launch and timer set; genuine remount remains a new demand. | Local implemented assertions PASS; see dated scope below |
| S03-T05 — lifecycle; S03-02/03 | JPB-007/013/017; B-10 | Hidden before initial load, hidden during headers/body/parse/publication, repeated visibility events, pagehide/pageshow persisted, initial pageshow, freeze/resume and unmount. Assert immediate masking, cancellation, generation advance, zero hidden ticks/requests, no early resume retry, next strictly future slot, no stale response restoration. Separate synthetic event tests from actual tab switch/back navigation/BFCache/device resume proof; record BFCache eligibility and whether restoration really occurred. | PARTIAL: local assertions PASS; manual/device lanes NOT RUN |
| S03-T06 — independent age; S03-03 | JPB-010/013/017; B-10 | Deterministic header+full request+processing+post-receipt budget. Test 29,999 / 30,000 / 30,001 ms at use, at first stale receipt and while a poll fails; timer independent of polling and no response after expiry. Exercise ceiling/saturation, delayed timers/render, monotonic pause with forward wall delta, forwards/backwards wall jumps and nonfinite/regressing clock. Never reset on HTTP/network failure or unchanged stale response; invalid timing masks until a new valid anchor. Exact-value details and old hostname also disappear. | Local implemented assertions PASS; see dated scope below |
| S03-T07 — atomic view states; S03-03/05 | JPB-009/017/018; B-01 consumer, B-10 | Complete→partial replaces nulls/issues atomically; valid current/stale/none, never-success, expired success and recovery. Distinguish Health failure HTTP 200, received Board HTTP error, timeout/network error and contract failure. Old Health availability changes to unknown/last-reported after refresh failure; none clears eligible old metrics immediately. No field-level merging, fabricated zeros, historic list or localStorage. | Local implemented assertions PASS; see dated scope below |
| S03-T08 — formatting/issues; S03-04 | JPB-002/015/016/019; B-03 display, B-11 | Exact decimal 0, 2^53−1/2^53/2^53+1, uint64 max; bigint ratio and rounding ties, total zero/null, unit carries through EiB, huge uptime days. CPU 0.5037 → 0.5%, floating integer-looking tokens, finite extreme floats and display -0. Null/false/true, unknown state string vs null. One issue per group, four real firmware paths, current interface indices, temperature independent; message text never interpreted as HTML or health severity. | Local implemented assertions PASS; see dated scope below |
| S03-T09 — components/accessibility; S03-05 | JPB-001/002/004/018/019; B-11 | Static structure plus browser DOM interactions for all state rows. Every prescribed metric/label visible, native details exact values, 64 interfaces and long messages, meaningful headings/labels, stable focus/open-details during refresh, focused-row removal. Keyboard-only interaction; polite deduplicated state announcements, none per normal poll/age tick, no color-only signal. Screen-reader operator check of transitions/association; automated rules alone insufficient. | PARTIAL: local assertions PASS; manual/device lanes NOT RUN |
| S03-T10 — embedded production integration; S03-06 | JPB-006/007/013/015/017/021; B-03/B-10/B-11, A-04 subset | Build actual frontend then Go, checksum binary/assets, serve embedded production page against controlled Health. Complete/partial/failure/recovery; stop Board after success and observe independent disappearance without server response. Unit fake-clock proof plus one real-browser elapsed-expiry run (condition waits bounded at 35 s, not arbitrary sleeps); no Pi latency claim. Count overview vs outbound Health attempts independently. Block non-origin requests and capture attempted requests/CSP violations/console; no CDN/fonts/telemetry, no inline-style/script CSP relaxation. Cold navigation with WAN blocked but local services available. Separate intercepted invalid Board responses from real Go HTTP chain. | Local implemented assertions PASS; see dated scope below |
| S03-T11 — targets and browser floors; S03-06 | JPB-005/013/015/017/021; B-03/B-10/B-11 | Execute critical parsing/exact-value/expiry/lifecycle suite at Chrome/Edge 111, Firefox 115, Safari/iOS Safari 16.4 and current-family smoke. Record actual patch/OS/device, not only user agent/emulated device name. Chromium/Firefox/WebKit CI is complementary; WebKit is not Safari, Node/static DOM is not browser. Follow availability/closure rules in Sprint 03; missing required target stays NOT RUN and blocks closure. | NOT RUN: required branded/floor/device lanes |
| S03-T12 — visual/mobile proof; S03-05/06 | JPB-002/015/018/019; B-03 display/B-11 | Reviewed screenshots at 360/390, 768, 1280 CSS px, 200% browser zoom/text enlargement, keyboard focus and contrast checks. Include nominal/partial/stale/none/Board HTTP/network failure, exact large counters expanded/visible, all-null groups, true firmware, long host/interface/issues and 64 interfaces. No horizontal overview overflow or hidden information; verify reading order. Actual Safari/iOS touch/background/back-navigation check as well as emulation. Record image IDs, case, viewport/zoom/DPR, source/binary hashes, browser and reviewer outcome; a screenshot without review is not PASS. | PARTIAL: local assertions PASS; manual/device lanes NOT RUN |
| S03-T13 — regression/build/cost; S03-07 | JPB-003/004/005/020/021/023; A-01–A-04 subsets | Locked install/tools; format/types/lint/Go vet/modules; S01 shared/fuzz and both interop paths; S02 suites/race; native+ARM64; frontend-before-embed, missing-dist and autonomous smoke retained. Measure actual parser-importing dashboard and first-overview-inclusive preliminary size totals per Sprint 03, <=250/500 KiB gzip budgets. Record raw and HTTP identity bytes separately, no claim of server compression or Pi performance. New browser dependency/pins reviewed and recorded; no opportunistic upgrades. | Local implemented assertions PASS; see dated scope below |
| S03-T14 — evidence/closure; S03-08 | JPB-024/025; sprint gate only | Exact source and binary/asset provenance, local vs remote/manual ledger, S03-T01–T13 evidence and target/visual matrix complete. Verify new hosted run, attempt/event/SHA/jobs/actual images/tools and browser revisions; no old S02 CI reuse. Preserve incidents/unavailable targets. No closure if a required target/visual/CI gate remains NOT RUN; no global A/B/C, S04 execution or release. | PENDING: hosted CI and mandatory manual evidence |

The current implementation instruction approves S03-R01/R02/R04 and the S03 P-06 presentation and preliminary size subset. The table above preserves the prepared oracles; it does not override the dated execution ledger below. Include stable IDs in future test names/comments or a maintained evidence map. Deterministic unit tests use fake streams/timers and barriers; real browser/network tests use readiness conditions and bounded observable waits. They must not depend on unbounded sleeps or claim fake-clock timings as platform measurements.

The closure boundary is explicit: S03 owns browser-floor execution and software visual/mobile acceptance, while S04 owns installed ARM64/Pi real-Health repetition, compressed delivery and all physical C gates. Absence of Apple/minimum-version infrastructure is an evidence gap, not a passed portability check. All preparation-time environment inventory and source inspection is documentary evidence only.

## S03 implementation traceability

The prepared matrix above is the full acceptance checklist, not a blanket PASS statement. [S03 evidence](s03-evidence.md) owns dated local results and exact source/binary identity. S00/S01/S02 CI runs are historical only. The current S03 workflow is implemented but has not executed remotely for these uncommitted changes.

| Prepared IDs | Implemented assertion owners | Execution boundary |
|---|---|---|
| S03-T01/T02/T03 | overview/contract.test.ts; S01 corpus; request/contract/json; browser shared valid corpus and maximum markup cases | Local unit/browser assertions recorded in S03 evidence. Strict wrapper, exact source, frozen projection and independent byte/depth profiles. |
| S03-T04/T05 | overview/controller.test.ts; browser actual development StrictMode and synthetic pagehide/pageshow | Local deterministic/engine checks; [2026-09-26 scheduling regression](s03-evidence.md#2026-09-26--early-cadence-callback-correction) adds fractional/early/late/consumed timer cases. Actual tab suspension/BFCache/device evidence still required. |
| S03-T06/T07 | controller paired-segment/expiry/state tests; real Board-stop and provider partial/failure/recovery/browser error lanes | Local unit/engine results; real device suspension remains NOT RUN. No Pi timing claim. |
| S03-T08 | presentation/format.test.ts, App.test.tsx; exact browser counters | Bigint rounding/ratios/EiB/uptime, null/zero/false and CPU percentage semantics. |
| S03-T09/T12 | App static semantics; browser keyboard/details/focus, 360/390/768/1280 reflow, 200% text, 64 interfaces and long issue text; reviewed screenshots | Automated/visual subsets only. Screen reader, actual native browser 200% zoom and Safari/iOS/operator work remain open. |
| S03-T10 | scripts/browser.spec.mjs + browser-harness.mjs, production Go binary | Real controlled Health HTTP → Board HTTP → browser; interception and development StrictMode explicitly separate. |
| S03-T11 | playwright.config.ts / playwright-browsers.json | Current automated engines are supplemental. All minimum-version and actual Safari/iOS proof remains NOT RUN; branded Windows tools were inaccessible. |
| S03-T13 | tasks check/build/cross/race/smoke/missing-dist/fuzz/sizes/browser; dashboard-sizes.mjs; CI browser job | Local regression/size results recorded; workflow changes are not hosted CI PASS. |
| S03-T14 | source/asset/binary manifest, browser ledger, operator checklist | PENDING: new exact-commit CI and all mandatory target/manual lanes. No complete A/B/C acceptance. |

Requirements remain assigned to the prepared IDs above and the stable requirement table. Browser counts and screenshots do not waive unexecuted rows. P-06 physical/compression protocols, packaging/systemd and all Raspberry Pi acceptance remain S04/NOT RUN.
